# Contributing

## Style
- Prefer small focused classes with deterministic behavior.
- Keep algorithms dependency-light and easy to inspect in interviews.
- Every new config parameter should be documented in `docs/config_reference.md`.

## Pull request checklist
- add or update tests when behavior changes
- update README and docs if a new app/config is introduced
- include sample output if a benchmark report format changes
