# Interview talking points

- Why global greedy can outperform plain nearest neighbor during crossing targets
- Why promotion/deletion thresholds matter for false-track suppression
- How mixed sensor confidence can be normalized before association
- How benchmark-style comparison is more convincing than “it runs on one CSV”
- What would change for JPDA, IMM, or a nonlinear filter in production
